@model string
@{
    ViewData["Title"] = "Home Page";
}

<div class="page-content page-container" id="page-content">
    <div class="padding">
        <div class="row container d-flex justify-content-center">
            <div class="col-md-9">
                <div class="card card-bordered">
                    <div class="card-header">
                        <div class="row">
                            <div class="col-4">
                                <h4 class="card-title"><strong>Chat</strong></h4>
                            </div>
                            <div class="col-6">
                                <input type="text" class="form-control" placeholder="Digite seu nome" id="userName" />
                            </div>
                            <div class="col-2">
                                <button type="button" class="btn btn-sm btn-primary" onclick="openChat()">
                                    Participar
                                </button>
                            </div>
                        </div>
                    </div>
                    <div class="ps-container ps-theme-default ps-active-y" id="chat-content" style="overflow-y: scroll !important; height:400px !important;">

                    </div>
                    <div class="publisher bt-1 border-light">
                        <img class="avatar avatar-xs" src="https://img.icons8.com/color/36/000000/administrator-male.png" alt="...">
                        <input class="publisher-input" type="text" placeholder="Digite sua Mensagem" id="MessageField">
                        <button type="button" onclick="sendMessage()" class="btn btn-sm btn-dark text-info" id="sendMessage">
                            Enviar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        let userName = '';
        let socket = null;
        let btnSend = $("#sendMessage");

        window.onload = function () {
            var wsUri = "ws://localhost:54068/public";
            socket = new WebSocket(wsUri);
            socket.onopen = e => {
                console.log("socket opened", e);
            };
            socket.onclose = function (e) {
                console.log("socket closed", e);
            };
            socket.onmessage = function (e) {
                console.log(e);
                $('#chat-content').append(createMessage(e.data));
            };
            socket.onerror = function (e) {
                console.error(e.data);
            };
        }

        function openChat() {
            userName = $('#userName').val();
            $('#userName').text('');
            $('#userName').val(null);
        }

        function sendMessage() {
            var message = userName + ": " + $('#MessageField').val();
            socket.send(message);
            $('#MessageField').val('');
        }

        function createMessage(message) {
            return `<div class="media media-chat">
                                    <img class="avatar" src="https://img.icons8.com/color/36/000000/administrator-male.png" alt="...">
                                    <div class="media-body">
                                        <p>${message}</p>
                                    </div>
                                </div>`;
        }

        function createReverseMessage(message) {
            return `<div class="media media-chat media-chat-reverse">
                                    <div class="media-body">
                                        <p>${message}</p>
                                    </div>
                                </div>`;
        }
    </script>
}